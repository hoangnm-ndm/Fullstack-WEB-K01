/**
 * some()
 */

const arr = [1, 2, 4, 6, 8, 10];

const checkEven = arr.some((item) => item % 2 === 0);

const students = [
	{ id: 1, name: "hoang", gender: "male" },
	{ id: 2, name: "tu", gender: "female" },
	{ id: 3, name: "quynh anh", gender: "female" },
	{ id: 4, name: "cuong", gender: "other" },
];

/**
 * * Kiem tra xem mang students có nữ không, nếu có trả về true -> some()
 * *
 */

/**
 * * Kiem tra xem nếu cả lớp là nam thì trả về true -> every()
 */
